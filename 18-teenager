public Boolean isTeenager(Integer age) {
    if (age >= 13 && age <= 19) {
        return true; // Age is in the teenager range
    } else {
        return false; // Age is outside the teenager range
    }
}
